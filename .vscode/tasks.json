{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Remove MongoDB data",
			"type": "shell",
			"command": "Remove-Item ${workspaceFolder}${pathSeparator}data -Force -Recurse; mkdir data; cd data ; echo . > .gitkeep;",
			"problemMatcher": []
		}
	]
}